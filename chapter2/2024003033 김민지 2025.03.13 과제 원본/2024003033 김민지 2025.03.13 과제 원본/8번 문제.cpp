#include <iostream>
using namespace std;
#include <cstring>
#include <string>

//8번 문제
/*
int main() {
	char name[100];
	string longname;
	int length = 0;

	cout << "5명의 이름을 ';'으로 구분하여 입력하세요>>";
	for (int i = 0; i < 5; i++) {
		cin.getline(name, 100, ';');
		cout << i+1 << " : " << name << "\n";
		if (strlen(name) > length) {
			length = strlen(name);
			longname = name;
		}
	}
	cout << "가장 긴 이름은 " << longname;

	return 0;
}
*/
